# Vamos supor que queiramos realizar as seguintes somas no Python
# 1 y = 0.1 + 0.1 + 0.1 sabemos que o resultado é 0,3;
# 2 y = 0.2 + 0.2 + 0.2 sabemos que o resultado é 0,4;
# Agora irei por o código da falcudade, e logo em seguinda irei corrigir os erros

# Soma 1
# soma = 0.1 + 0.1 + 0.1
# print('soma = '+str(val)) # Observe que temos um em "val", logo após iremos substituí-lo por "soma"

soma =  0.1 + 0.1 + 0.1
print('soma = '+str(soma)) # Vejamos o que acontece quando substituímos "val" por "soma"